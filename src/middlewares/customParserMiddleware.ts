/*
export function customSensorMacsParser(req, res, next) {
  if (typeof req.query.sensorMacs === "string" && req.query.sensorMacs.trim() === "") {
    delete req.query.sensorMacs;
  }
  next();
}

export function customStartDateParser(req, res, next) {
  if (typeof req.query.startDate === "string" && req.query.startDate.trim() === "") {
    delete req.query.startDate;
  }
  next();
}

export function customEndDateParser(req, res, next) {
  if (typeof req.query.endDate === "string" && req.query.endDate.trim() === "") {
    delete req.query.endDate;
  }
  next();
}
*/